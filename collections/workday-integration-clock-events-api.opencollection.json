{
  "opencollection": "1.0.0",
  "info": {
    "name": "Workday Integration Workday Absence Management Absence Types Clock Events API",
    "version": "v1"
  },
  "request": {
    "auth": {
      "type": "oauth2",
      "flow": "authorization_code",
      "authorizationUrl": "https://{baseUrl}/authorize",
      "accessTokenUrl": "https://{baseUrl}/oauth2/{tenant}/token",
      "credentials": {
        "clientId": "{{clientId}}",
        "clientSecret": "{{clientSecret}}"
      }
    }
  },
  "items": [
    {
      "info": {
        "name": "Clock Events",
        "type": "folder"
      },
      "items": [
        {
          "info": {
            "name": "Workday Integration Create a time clock event",
            "type": "http"
          },
          "http": {
            "method": "POST",
            "url": "https://{baseUrl}/ccx/api/absenceManagement/v1/{tenant}/timeClockEvents",
            "body": {
              "type": "json",
              "data": "{}"
            }
          },
          "docs": "Records a time clock event such as a clock-in or clock-out for a worker."
        }
      ]
    }
  ],
  "bundled": true
}